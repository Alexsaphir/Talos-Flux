---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: &app recyclarr
  namespace: media
spec:
  components:
    - ../../../../components/volsync

  postBuild:
    substitute:
      APP: *app
      PVC_NAME: *app
      VOLSYNC_CAPACITY: 10Gi

  prune: true
  interval: 1h
  path: cluster/apps/media/arr/recyclarr
  sourceRef:
    kind: GitRepository
    name: talos-kubernetes
    namespace: flux-system

---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: seerr
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: bitwarden

  target:
    name: seerr-secret
    template:
      data:
        API_KEY: '{{ .SEER_API_KEY }}'

  dataFrom:
    - extract:
        key: seer

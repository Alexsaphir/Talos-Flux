---
apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: calibre-scrapper-daily
spec:
  schedule: '@daily'
  immediate: true
  backupOwnerReference: self
  cluster:
    name: calibre-scrapper

  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io

---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: grafana-mcp
  namespace: ai
spec:
  dependsOn:
    - name: toolhive-config
      namespace: ai

  prune: true
  interval: 1h
  path: cluster/apps/ai/mcp/grafana-mcp
  sourceRef:
    kind: GitRepository
    name: talos-kubernetes
    namespace: flux-system

---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: old-postgres
  namespace: &namespace database
spec:
  dependsOn:
    - name: cloudnative-pg
      namespace: cloudnative-pg

  healthChecks:
    - apiVersion: v1
      kind: Pod
      name: postgres-1
      namespace: *namespace

  prune: true
  interval: 1h
  path: cluster/infra/database/old-postgres
  sourceRef:
    kind: GitRepository
    name: talos-kubernetes
    namespace: flux-system

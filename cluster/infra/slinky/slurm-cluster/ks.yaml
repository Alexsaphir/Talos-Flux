---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: &app slurm-cluster
  namespace: &namespace slinky
spec:
  healthChecks:
    - apiVersion: helm.toolkit.fluxcd.io/v2
      kind: HelmRelease
      name: *app
      namespace: *namespace

  dependsOn:
    - name: slurm-operator

  prune: true
  interval: 1h
  path: cluster/infra/slinky/slurm-cluster
  sourceRef:
    kind: GitRepository
    name: talos-kubernetes
    namespace: flux-system

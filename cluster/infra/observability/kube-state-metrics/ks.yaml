---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: &app kube-state-metrics
  namespace: &namespace observability
spec:
  healthChecks:
    - apiVersion: helm.toolkit.fluxcd.io/v2
      kind: HelmRelease
      name: *app
      namespace: *namespace

  prune: true
  interval: 1h
  path: cluster/infra/observability/kube-state-metrics
  sourceRef:
    kind: GitRepository
    name: talos-kubernetes
    namespace: flux-system
